# Self-Improvement Retro (2025-11-18)
- Consolidated requirements/docs before coding to avoid context-switching later.
- Added docs (`TESTING.md`, `QA_NOTES.md`, `CHANGELOG.md`) so future HA validation has a playbook—even when tooling isn’t installed yet.
- Emphasized reusability by relying on recorded API snapshots for coordinator tests instead of stubs.
- Next time: bootstrap HA dev container earlier to run hassfest/HA pytest before implementing platforms.
